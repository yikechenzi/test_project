# AI自动化测试平台 - 小白部署教程

> 本文档面向零基础用户，手把手教你部署和运行AI自动化测试平台。

---

## 一、环境准备

在开始之前，你需要在电脑上安装以下软件：

### 1.1 安装 Docker Desktop

Docker 是运行本项目的核心工具，它可以自动帮你搭建数据库、后端、前端等所有服务。

**Windows 安装步骤：**

1. 打开浏览器，访问 [Docker Desktop 官网](https://www.docker.com/products/docker-desktop/)
2. 点击 **Download for Windows** 按钮下载安装包
3. 双击下载的安装包，按照提示一路点击 **Next** 安装
4. 安装完成后，双击桌面图标启动 Docker Desktop
5. 等待左下角出现绿色的 **Engine running** 字样，表示 Docker 已启动成功

**验证安装：**

打开 PowerShell（按 `Win + X`，选择 Windows PowerShell），输入：

```powershell
docker --version
```

如果显示类似 `Docker version 24.x.x` 的版本号，说明安装成功。

### 1.2 安装 Git

Git 用于下载项目代码。

1. 访问 [Git 官网](https://git-scm.com/downloads)
2. 下载 Windows 版本安装包
3. 双击安装，一路 **Next** 即可（默认选项不需要修改）

**验证安装：**

```powershell
git --version
```

显示 `git version 2.x.x` 即为成功。

---

## 二、获取项目代码

### 2.1 克隆仓库

打开 PowerShell，依次执行以下命令：

```powershell
# 进入你想存放项目的目录（例如桌面）
cd C:\Users\你的用户名\Desktop

# 克隆项目代码
git clone https://github.com/yikechenzi/test_project.git -b test

# 进入项目目录
cd test_project\ai-test-platform
```

> 如果提示输入用户名和密码，请输入你的 GitHub 账号和密码（或 Personal Access Token）。

---

## 三、配置环境变量

### 3.1 复制配置文件

在项目根目录下，找到 `.env.example` 文件，复制一份并重命名为 `.env`：

```powershell
copy .env.example .env
```

### 3.2 编辑配置文件

用记事本打开 `.env` 文件：

```powershell
notepad .env
```

**必须修改的配置项：**

```env
# JWT密钥 - 改成一个随机字符串（至少32位）
SECRET_KEY=my-super-secret-key-change-this-to-something-random-12345

# OpenAI API密钥（如果要使用AI功能，需要填入你的API Key）
# 如果没有，可以先留空，其他功能不受影响
OPENAI_API_KEY=

# 数据库密码（默认即可，本地开发无需修改）
POSTGRES_PASSWORD=postgres123
```

> **注意**：`SECRET_KEY` 一定要修改，不要使用默认值，这是安全密钥。

保存并关闭文件。

---

## 四、启动项目

### 4.1 使用 Docker Compose 启动（推荐）

在 PowerShell 中，确保你在项目根目录下：

```powershell
# 确认当前目录
pwd
# 应该显示: C:\Users\你的用户名\...\ai-test-platform

# 启动所有服务
docker-compose up -d
```

> 第一次启动会下载所有依赖镜像，可能需要 **5-15 分钟**，请耐心等待。

### 4.2 查看启动状态

```powershell
# 查看所有容器状态
docker-compose ps
```

正常情况下，你应该看到以下容器都是 `Up` 状态：

| 容器名 | 状态 | 说明 |
|--------|------|------|
| ai-test-postgres | Up | 数据库 |
| ai-test-redis | Up | 缓存 |
| ai-test-backend | Up | 后端API |
| ai-test-frontend | Up | 前端页面 |

### 4.3 或者使用启动脚本

双击项目根目录下的 `start.bat` 文件，按提示操作即可。

---

## 五、访问项目

启动成功后，打开浏览器访问：

| 地址 | 说明 |
|------|------|
| http://localhost:5173 | 前端页面（主要使用这个） |
| http://localhost:8000 | 后端API |
| http://localhost:8000/docs | API文档（Swagger UI） |

### 5.1 登录系统

1. 打开 http://localhost:5173
2. 在登录页面输入：
   - 用户名：`admin`
   - 密码：`admin123`
3. 点击 **登录** 按钮

---

## 六、基本使用流程

### 6.1 创建公司

1. 登录后，点击左侧菜单 **公司管理**
2. 点击右上角 **新建公司** 按钮
3. 输入公司名称和描述
4. 点击 **确定** 保存

### 6.2 创建项目

1. 点击左侧菜单 **项目管理**
2. 点击 **新建项目**
3. 填写项目名称、选择所属公司
4. 点击 **确定** 保存

### 6.3 编写测试用例

1. 点击左侧菜单 **测试用例**
2. 点击 **新建用例**
3. 填写用例名称、选择项目和测试类型
4. 在 **测试脚本** 区域编写Python测试代码，例如：

```python
# 简单的UI测试示例
import asyncio

async def test_example(page):
    # 打开百度首页
    await page.goto("https://www.baidu.com")
    
    # 在搜索框中输入
    await page.fill("#kw", "AI自动化测试")
    
    # 点击搜索按钮
    await page.click("#su")
    
    # 等待页面加载
    await page.wait_for_timeout(3000)
    
    # 验证搜索结果
    title = await page.title()
    print(f"页面标题: {title}")
```

### 6.4 执行测试

1. 在测试用例列表中，点击某个用例的 **执行** 按钮
2. 系统会自动启动浏览器执行测试
3. 可以在 **测试执行** 页面查看执行状态和日志

### 6.5 查看测试报告

1. 点击左侧菜单 **测试报告**
2. 选择项目后点击 **生成报告**
3. 系统会自动统计测试结果并生成可视化报告

### 6.6 添加用户

1. 点击左侧菜单 **用户管理**
2. 点击 **新增用户**
3. 填写用户名、邮箱、密码
4. 选择角色（管理员/普通用户）
5. 点击 **绑定项目**，选择用户可访问的项目
6. 普通用户只能看到被绑定项目的数据

---

## 七、常见问题

### Q1: Docker 启动报错 "Cannot connect to the Docker daemon"

**解决方案：**
- 确保 Docker Desktop 已经启动（图标在系统托盘中）
- 等待 Docker Engine 完全启动（左下角显示绿色 running）
- 重启 Docker Desktop

### Q2: 端口被占用（5173 或 8000）

**解决方案：**

```powershell
# 查看端口占用情况
netstat -ano | findstr :5173
netstat -ano | findstr :8000

# 杀掉占用端口的进程（PID 替换为上面命令显示的数字）
taskkill /PID <PID> /F
```

或者修改 `docker-compose.yml` 中的端口映射。

### Q3: 数据库连接失败

**解决方案：**

```powershell
# 检查 postgres 容器是否正常运行
docker-compose ps postgres

# 如果状态异常，重启容器
docker-compose restart postgres

# 如果还是不行，清理并重建
docker-compose down -v
docker-compose up -d
```

### Q4: 前端页面打不开（空白页）

**解决方案：**

```powershell
# 检查前端容器日志
docker-compose logs frontend

# 重启前端容器
docker-compose restart frontend

# 清除浏览器缓存后重试
```

### Q5: AI 功能无法使用

**解决方案：**
- 检查 `.env` 文件中的 `OPENAI_API_KEY` 是否已正确配置
- 确认 OpenAI API 账户有足够余额
- 检查网络是否能访问 OpenAI 服务
- 如果使用代理，需要在 Docker 容器中配置代理环境变量

### Q6: 测试执行失败，提示找不到 Playwright 浏览器

**解决方案：**

```powershell
# 进入后端容器安装浏览器
docker-compose exec backend playwright install chromium

# 然后重新执行测试
```

---

## 八、停止项目

### 8.1 停止所有服务

```powershell
cd C:\Users\你的用户名\...\ai-test-platform
docker-compose stop
```

### 8.2 停止并删除容器（保留数据）

```powershell
docker-compose down
```

### 8.3 停止并删除所有数据（完全清理）

```powershell
# 警告：这会删除数据库中的所有数据！
docker-compose down -v
```

### 8.4 或使用停止脚本

双击项目根目录下的 `stop.bat` 文件。

---

## 九、不使用 Docker 的本地开发方式

如果你不想使用 Docker，也可以手动启动后端和前端。

### 9.1 安装 PostgreSQL 数据库

1. 访问 [PostgreSQL 官网](https://www.postgresql.org/download/windows/)
2. 下载并安装 PostgreSQL
3. 安装过程中设置密码（记住这个密码）
4. 安装完成后，打开 pgAdmin 或命令行创建数据库：

```powershell
# 连接 PostgreSQL
psql -U postgres

# 创建数据库
CREATE DATABASE ai_test_db;

# 退出
\q
```

### 9.2 安装 Redis

1. 访问 [Redis for Windows](https://github.com/tporadowski/redis/releases)
2. 下载最新版本并解压
3. 双击 `redis-server.exe` 启动 Redis

### 9.3 启动后端

```powershell
cd backend

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 安装 Playwright 浏览器
playwright install chromium

# 启动后端服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 9.4 启动前端

打开 **新的** PowerShell 窗口：

```powershell
cd frontend

# 安装依赖（需要安装 Node.js 18+）
npm install

# 启动前端开发服务器
npm run dev
```

---

## 十、项目技术栈总结

| 组件 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue.js | 3.x |
| UI组件库 | Element Plus | 2.x |
| 状态管理 | Pinia | 2.x |
| 后端框架 | FastAPI | 0.104.x |
| 数据库 | PostgreSQL | 15 |
| 缓存 | Redis | 7.x |
| 测试引擎 | Playwright | 1.40.x |
| AI服务 | OpenAI API | GPT-4 |
| 容器化 | Docker | 24.x |

---

## 十一、获取帮助

如果遇到问题：

1. 查看项目根目录下的 `README.md` 文档
2. 查看后端日志：`docker-compose logs backend`
3. 查看前端日志：`docker-compose logs frontend`
4. 在 GitHub 仓库提交 Issue

---

## 十二、快速命令参考

```powershell
# 启动项目
docker-compose up -d

# 停止项目
docker-compose down

# 查看日志
docker-compose logs -f

# 查看某个服务的日志
docker-compose logs -f backend
docker-compose logs -f frontend

# 重启某个服务
docker-compose restart backend

# 重建所有服务
docker-compose down
docker-compose up -d --build

# 进入后端容器
docker-compose exec backend bash

# 进入数据库
docker-compose exec postgres psql -U postgres -d ai_test_db
```

---

> 祝你使用愉快！如有问题，欢迎反馈。
